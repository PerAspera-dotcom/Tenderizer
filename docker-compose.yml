services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: tenderizer
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: tenderizer
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
